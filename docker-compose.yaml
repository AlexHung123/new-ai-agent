services:
  aiagent:
    image: itms-ai-agent:v1
    ports:
      - '3001:3000'
    volumes:
      - data:/home/aiagent/data
      - uploads:/home/aiagent/uploads
    restart: unless-stopped

volumes:
  data:
    name: 'aiagent-data'
  uploads:
    name: 'aiagent-uploads'
